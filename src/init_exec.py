# === INIT EXECUTER ============================================================
# given an identifier, type and expression, returns a named symbol with identifier
# as a name and expression as its value, if the types match

from parser import Symbol

def init_exec() -> Symbol:
    pass